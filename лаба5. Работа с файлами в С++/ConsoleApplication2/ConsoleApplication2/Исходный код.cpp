#include <stdio.h>  
#include <string>
#include <string.h>
#include <iostream>
#include <fstream>
using namespace std; 
int main ()
{/*    int i=0, l=0, m=0;

		char str1[255],str2[255], a[100];
		string f[100];
      fstream fin;
	  fin.open ("F1.txt");
      ofstream fout1 ("F2.txt");  
      while (!fin.eof())
      {    char s[255] = "";
           fin.getline(a,100,'\n');
		   f[i]=a;
		   i++;
		}
fin.close();
			
			for(l;l<i;l++)
			{
				
			}

           /*if (strlen(s) <= k)    
                 fout1 << s;
           else
                for (int i = strlen(s) - k - 1; i < strlen(s);  i++)
                     fout1 << s[i];       
      }   

fout1.close ();      */
	std::wcout << L"Hello WCHAR" << std::endl ;
return 0;     
}
